#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

static int len(const char *s1)
{
    int i;
    i = 0;
    while(s1[i] != '\0')
        i++;
}

char *ft_strtrim(char const *s1, char const *set)
{
   int i;
   int j;
   char *str;
   i = 0;
   j = 0;
   str = (char*)malloc(len(s1));
   while (*(s1 + i) != '\0')
   {
    j = 0;
    while(*(set + j) != '\0')
    {
        if ((*s1 + i) == (*set + j))
        {
            
        }
    }
   }

}
